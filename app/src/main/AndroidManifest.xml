<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.thezero.randomjokes">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RandomJokes"
        android:name=".app.RandomJokesApp">
        <activity
            android:name=".presentation.splashscreen.view.SplashActivity"
            android:exported="true"
            android:screenOrientation="userPortrait"
            tools:ignore="LockedOrientationActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.mainscreen.view.MainActivity"
            android:exported="false"
            android:screenOrientation="userPortrait"
            tools:ignore="LockedOrientationActivity"/>
    </application>

</manifest>